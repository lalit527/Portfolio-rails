<div class="album py-5 bg-light">
  <div class="container">

    <div class="row <%= "sortable" if logged_in?(:site_admin) %>">
      <%= render :partial => 'portfol', :collection => @portfolio_item, :as => :item  %>
    </div>
  </div>
</div>
