<h1>Portfolio Page</h1>

<%= link_to "Create New Item", new_portfol_path %>

<% @portfolio_item.each do |item| %>
  <p><%= link_to item.title, portfol_path(item) %></p>
  <p><%= item.subtitle %></p>
  <p><%= item.body %></p>
  <%= image_tag item.thumb_image unless item.thumb_image.nil? %>
  <%= link_to "Edit", edit_portfol_path(item) %>
  <hr>
<% end %>
